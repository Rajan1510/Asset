## Packages
papaparse | CSV parsing for import/export features
@types/papaparse | Types for papaparse
framer-motion | Smooth animations for UI transitions
recharts | Data visualization for the dashboard
lucide-react | Icons (standard in this stack, but listing for completeness)
date-fns | Date formatting

## Notes
- Authentication is handled via Replit Auth (useAuth hook)
- Asset import expects CSV with specific headers
- Export functionality generates CSV client-side
- Tailwind config should support the custom fonts defined in index.css
